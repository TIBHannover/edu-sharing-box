# (Required) Edu-Sharing host
ES_HOST_PROTO={{ edu_sharing_protocol | default("http") }}
ES_HOST_DOMAIN={{ edu_sharing_host }}

# (Required) Edu-Sharing User to publish Opencast content with
ES_USER={{ opencast_importer_es_user }}
ES_PASSWORD={{ opencast_importer_es_password }}

# Debug HTTP requests
#DEBUG=axios
